# Dependencias del panel de recobros (módulo autónomo).
# db.py, importer.py y sources/base.py solo usan la stdlib.
# El resto (logic, app, sources, sync) necesita:
pandas>=2.0
streamlit>=1.30
requests>=2.31        # conectores HubSpot / WooCommerce
openpyxl>=3.1         # lectura de subidas .xlsx
